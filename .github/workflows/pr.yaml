name: CI
on: [pull_request]
jobs:
  build:
    name: Pre-merge 
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Install modules
        run: yarn
      - name: Run ESLint
        run: yarn lint
      - name: Build
      - run: yarn build